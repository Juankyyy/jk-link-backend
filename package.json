{
	"name": "jk-link-backend",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"auth:hash": "node scripts/generate-bcrypt-hash.mjs",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest run"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.12.4",
		"vitest": "~3.2.0",
		"wrangler": "^4.81.0"
	},
	"dependencies": {
		"bcryptjs": "^3.0.3",
		"hono": "^4.12.12"
	}
}